
> @layeredapps/maxmind-geoip@1.0.0 test-sqlite
> npm install sqlite3 --no-save --no-audit --no-fund >/dev/null && NODE_ENV=testing STORAGE=sqlite SQLITE_DATABASE=dashboard SQLITE_FILE=`pwd`/db.sqlite mocha --bail './{,!(node_modules)/**}/*.test.js' --timeout 90000 --recursive



  server/bind-country
    before
      ✔ should bind data to req

  /api/user/geoip/country
    exceptions
      invalid-ip
        ✔ missing querystring ip
        ✔ invalid querystring ip
    receives
      ✔ required querystring ip
    returns
      ✔ object


  5 passing (375ms)

